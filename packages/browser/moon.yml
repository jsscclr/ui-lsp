tasks:
  build:
    command: pnpm build
    inputs:
      - 'src/**/*'
    outputs:
      - 'dist'
    deps:
      - 'shared:build'
  typecheck:
    command: pnpm typecheck
    inputs:
      - 'src/**/*'
    deps:
      - 'shared:typecheck'
